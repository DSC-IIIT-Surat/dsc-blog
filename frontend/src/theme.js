import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#2c84fb",
    },
    secondary: {
      main: "#fb2c25",
    },
  },
  background: {
    default: "#fff",
  },
});

export default theme;
